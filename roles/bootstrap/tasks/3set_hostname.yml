---
- name: Set hostname same as inventory_hostname
  ansible.builtin.hostname:
    name: "{{ inventory_hostname }}"

- name: Add hostname to /etc/hosts
  ansible.builtin.lineinfile:
    dest: /etc/hosts
    regexp: ^{{ ansible_host }}=
    line: "{{ ansible_host }} {{ inventory_hostname }}"
