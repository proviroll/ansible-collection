---
github_user_pubkeys:
  - aminechakr
  - zaguezzar
  - talbitalbi

old_github_user_pubkeys: []
ansible_user: ubuntu
bootstrap_user: devops
backup_user: loginbackup
initial_remote_user: ubuntu

ssh_port: 22 # NOTE: In case changing ssh port, consider updating 'geerlingguy.firewall' ports 
ssh_users:
  - "{{ bootstrap_user }}"
  - "{{ backup_user }}"
  - Abdelhadi
  - Amine
  - Zakaria

#### firewall task vars ####
setup_firewall: false
# list of TCP ports to allow incoming traffic
firewall_allowed_tcp_ports:
  - 22
firewall_allowed_udp_ports: []
