---
prometheus:
  web_port: 9090
  datasource_uid: prometheus

metrics_auth:
  user: '{{ metrics_basic_auth_user }}'
  password: '{{ prometheus_auth_password }}'

grafana_container_name: grafana
grafana_container_image_and_tag: grafana/grafana:11.6.1
