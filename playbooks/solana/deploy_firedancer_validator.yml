- name: Deploy solana validator
  hosts: all
  vars:
    network: solana
    network_type: testnet # Values : testnet, devnet, mainnet-beta
    chain_id: 
  become: true
  roles:
    - solana_firedancer