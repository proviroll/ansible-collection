---
- name: Upgrade solana firedancer validator
  hosts: all
  become: true
  vars:
    network: solana
    solana_previous_version : "v3.1.10"
    solana_version: "v0.902.40002" # Must be updated on 'solana_firedancer' role as well
    deployment_type: upgrade
    network_type: testnet # Values : testnet, devnet, mainnet-beta
  roles:
    - solana_firedancer
